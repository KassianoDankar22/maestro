// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("SUPABASE_URL_POSTGRES")
}

// TODO: Fazer migration do dado final dos scrappers
// TODO: Descobrir como os dados retornam (prevenir overwrites no que já temos)
// TODO: Fazer Dump do banco de dados